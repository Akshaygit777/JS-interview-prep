//what is an arrow function?

//A shorter syntax for normal function declaration. In case of single line function,
//  we don’t need to use the “return” keyword to return the value.


    // Ways to write an arrow function
    const sum = (a, b) => a + b;