//why is callback hell?
//callback is hell because it makes the code hard to read and maintain 
// when it consist of pattern like callbacks within callbacks

// This leads to deeply nested code structure that is difficult to follow and understand.

// Example of callback hell

// Callback hell example

//function (
   // anotherFunction(
        //anotherfunction();
    // )
// )
